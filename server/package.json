{
  "name": "risk-cockpit-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The desk's one backend: serves the UI and turns each save into a single commit on the repo. Contract in ../AGENTS.md.",
  "main": "proxy.mjs",
  "scripts": {
    "start": "node proxy.mjs"
  },
  "engines": { "node": ">=20" }
}
