# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# data/desk.json is the shared source of truth and IS tracked.
# Stray exports dropped anywhere else are not — commit deliberately, not by accident.
lionscraft-desk*.json
desk*.json
!data/desk.json
